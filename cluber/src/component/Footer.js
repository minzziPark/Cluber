import style from "../css/Footer.module.css";

export default function Footer() {
  return (
    <div className={`${style.footer_box}`}>
      <div className={`${style.footer}`}>Handong Global University Club</div>
    </div>
  );
}
